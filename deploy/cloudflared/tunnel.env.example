# Copier vers tunnel.env (gitignored). Ne jamais committer.
# Token obtenu dans Cloudflare Zero Trust → Networks → Tunnels → votre tunnel → Install connector

CLOUDFLARE_TUNNEL_TOKEN=

# URL publique du Chatbot (pour scripts/verify-deployment.mjs)
PUBLIC_CHATBOT_URL=https://chatbot.votre-domaine.fr
